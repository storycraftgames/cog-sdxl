# Configuration for Cog ⚙️
build:
  gpu: true
  cuda: "11.8"
  python_version: "3.9"
  python_packages:
    - "torch==2.0.1" # Ensure compatibility with your CUDA version

predict: "predict.py:Predictor"
